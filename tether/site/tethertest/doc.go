// Package tethertest demonstrates the tethertest harness - Tether's
// unit testing tool for handler logic. The tests in this package
// exercise every harness method using real handlers from the example
// application, showing developers how to test their own code without
// a browser, HTTP server, or transport layer.
package tethertest
