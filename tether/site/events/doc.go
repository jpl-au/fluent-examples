// Package events demonstrates every Tether event binding type:
// click, submit, input, change, keydown, focus, blur, viewport,
// throttle, debounce, confirm, custom DOM events, typed extraction
// (ev.Int, ev.Float64, ev.Bool), and struct binding (ev.Bind).
// Stateless HTTP - no persistent connection needed.
package events
